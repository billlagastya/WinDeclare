# Project Rules

## Git & Deployment Directives
- **Do NOT execute `git push` or deploy changes automatically.**
- Only modify files locally in the workspace.
- The user will manually test changes on `http://localhost:3000` and perform git commits/pushes when ready.
